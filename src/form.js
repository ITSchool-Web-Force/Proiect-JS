//Formuri
const form = document.getElementById('the-form');

form.addEventListener("submit", (event) => {
        event.preventDefault();
        alert('Site-ul încă nu este complet funcțional. Revin-o in viitor, cine știe...');
});